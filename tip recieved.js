if ($room.owner === 'hannasthesia' || $app.version === 'Testbed'){
addTokies($user.username, $tip.tokens)
if (gamerunning() && getUserList()[$user.username] && getUserList()[$user.username].participant && $tip.tokens === letterTip)handleGuess(handleTipnote($tip.message))
}
